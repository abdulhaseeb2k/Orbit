<?xml version="1.0" encoding="utf-8"?>
<!--
   Backup / transfer rules for API 31+.
   auth.xml holds the Firebase id token AND the long-lived refresh token in
   cleartext. Excluded from both cloud backup and device-to-device transfer:
   anyone restoring that file would hold a credential the app never revokes.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="auth.xml"/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="auth.xml"/>
    </device-transfer>
</data-extraction-rules>
